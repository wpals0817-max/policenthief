# Policenthief 코드 리뷰 결과

**리뷰 일시**: 2026-02-11  
**빌드 상태**: ✅ 성공 (Next.js 16.1.6)  
**TypeScript 상태**: ✅ 타입 오류 없음

---

## 📊 전체 평가

| 항목 | 상태 | 점수 |
|------|------|------|
| 프로젝트 구조 | ✅ 양호 | 85/100 |
| 타입 정의 | ✅ 우수 | 90/100 |
| 컴포넌트 설계 | ✅ 양호 | 80/100 |
| 기능 완성도 | ⚠️ 미완성 | 60/100 |
| 에러 처리 | ⚠️ 부족 | 50/100 |
| PWA 설정 | ❌ 미완성 | 40/100 |
| Firebase 연동 | ⚠️ 부분 구현 | 55/100 |

**종합 점수**: 66/100 (보통)

---

## ✅ 잘된 점

### 1. 프로젝트 구조
- Next.js 16 App Router 올바르게 사용
- 폴더 구조가 논리적이고 명확함
- 컴포넌트 분리가 적절함

### 2. 타입 시스템
- `src/types/index.ts`에 모든 타입이 체계적으로 정의됨
- TypeScript 타입 안정성 우수
- 인터페이스 설계가 일관성 있음

### 3. 상태 관리
- Zustand 사용으로 전역 상태 관리가 깔끔함
- `gameStore`, `profileStore` 분리가 적절함
- persist 미들웨어로 로컬 저장 구현

### 4. UI/UX
- Tailwind CSS로 일관된 디자인
- 다크 테마 적용
- 경찰/도둑 색상 구분이 명확함
- 반응형 디자인 고려됨

### 5. 지도 통합
- Leaflet 동적 로드로 SSR 문제 해결
- 커스텀 마커와 경계 표시 잘 구현
- 지도 UI가 직관적임

---

## ⚠️ 발견된 주요 문제점

### 🔴 심각 (Critical)

#### 1. Firebase 실시간 동기화 미구현
**위치**: `src/lib/roomService.ts`, `src/app/game/[code]/page.tsx`

**문제**:
- Firebase Realtime Database가 선언만 되고 사용되지 않음
- 플레이어 위치가 실시간으로 공유되지 않음
- 게임 상태 변경이 다른 플레이어에게 즉시 반영되지 않음

**영향**: 멀티플레이어 게임의 핵심 기능이 작동하지 않음

**해결 필요**:
```typescript
// Realtime Database에 위치 업데이트
import { ref, set, onValue } from "firebase/database";

// 위치 공유
set(ref(rtdb, `games/${roomCode}/players/${playerId}/location`), location);

// 위치 구독
onValue(ref(rtdb, `games/${roomCode}/players`), (snapshot) => {
  // 실시간 위치 업데이트
});
```

#### 2. 자동 체포/구출 로직 미구현
**위치**: `src/app/game/[code]/page.tsx`

**문제**:
- 거리 기반 자동 체포가 없음 (수동 버튼 클릭만)
- 경찰과 도둑의 거리 계산 후 자동 체포 로직 없음
- 구출도 거리 체크 없이 버튼만 제공

**영향**: GPS 기반 게임의 핵심 기능 부재

**해결 필요**:
```typescript
// 게임 루프에서 주기적으로 거리 체크
useEffect(() => {
  if (gameStatus !== 'playing') return;
  
  const interval = setInterval(() => {
    checkPlayerDistances(); // 모든 플레이어 간 거리 계산
    checkBoundaryViolations(); // 경계 이탈 체크
  }, 2000); // 2초마다
  
  return () => clearInterval(interval);
}, [gameStatus]);
```

#### 3. PWA 아이콘 누락
**위치**: `public/icons/`

**문제**:
- `icon-192.png`, `icon-512.png` 파일이 없음
- `icon.svg`만 존재
- manifest.json은 PNG 아이콘을 참조하지만 실제 파일 없음

**영향**: PWA 설치가 제대로 작동하지 않음

#### 4. Service Worker 미설정
**위치**: 프로젝트 루트

**문제**:
- `sw.js` 또는 `service-worker.js` 파일 없음
- 오프라인 지원 불가
- PWA 캐싱 전략 없음

**영향**: PWA의 오프라인 기능과 성능 최적화 불가

---

### 🟡 중요 (Important)

#### 5. 경계 이탈 자동 탈락 미구현
**위치**: `src/app/game/[code]/page.tsx`

**문제**:
- 경계 이탈 경고만 표시되고 실제 탈락 처리 안됨
- `outOfBoundsWarning` 상태만 토글
- 플레이어 상태가 `disconnected`로 변경되지 않음

**현재 코드**:
```typescript
if (!withinBoundary) {
  setOutOfBoundsWarning(true);
  addNotification("⚠️ 경계를 벗어났습니다! 돌아오세요!");
  // 실제 탈락 처리 없음!
}
```

**해결 방법**:
```typescript
// 경계 밖 시간 추적 후 자동 탈락
if (!withinBoundary) {
  setOutOfBoundsDuration(prev => prev + 1);
  if (outOfBoundsDuration > 15) { // 15초 초과 시
    handleElimination();
  }
}
```

#### 6. 에러 처리 부족
**위치**: 여러 파일

**문제**:
- Firebase 오류 시 사용자에게 구체적인 메시지 없음
- 네트워크 오류 처리 미흡
- try-catch 블록이 있지만 단순 console.log만

**개선 필요**:
```typescript
try {
  await updateRoom(room);
} catch (error) {
  console.error(error); // 현재
  
  // 개선안
  if (error.code === 'permission-denied') {
    showError('권한이 없습니다');
  } else if (!navigator.onLine) {
    showError('인터넷 연결을 확인하세요');
  } else {
    showError('알 수 없는 오류가 발생했습니다');
  }
}
```

#### 7. 위치 권한 재요청 로직 부재
**위치**: `src/hooks/useGeolocation.ts`

**문제**:
- 권한 거부 시 재요청 방법 안내 없음
- iOS/Android 설정 페이지로 안내하는 링크 없음

#### 8. 게임 종료 조건 처리 미흡
**위치**: `src/app/game/[code]/page.tsx`

**문제**:
- 모든 도둑 체포 시 게임 종료는 구현됨
- 하지만 중간에 플레이어 연결 끊김 처리 없음
- 방장이 나가면 게임이 어떻게 되는지 불명확

---

### 🟢 경미 (Minor)

#### 9. Card 컴포넌트 variant 미사용
**위치**: `src/components/Card.tsx`, 사용처

**문제**:
```typescript
// Card.tsx에 police/thief variant가 정의됨
variant?: "default" | "glass" | "police" | "thief";

// 하지만 PlayerList에서만 사용하고 실제 스타일 적용 안됨
```

#### 10. 중복 코드
**위치**: `src/lib/roomService.ts`, `src/lib/roomUtils.ts`

**문제**:
- `formatDistance` 함수가 두 파일에 중복
- 플레이어 ID 생성 로직 중복 가능성

#### 11. 불필요한 주석
**위치**: 여러 파일

```typescript
// Firebase 없이도 작동하도록 localStorage 폴백 포함
// 이미 코드로 명확함
```

#### 12. Magic Number 사용
**위치**: 여러 파일

```typescript
// 나쁜 예
setTimeout(() => {}, 3000);
if (distance > 100) { }

// 좋은 예
const NOTIFICATION_DURATION = 3000;
const CATCH_DISTANCE = 100;
```

#### 13. useCallback/useMemo 최적화 부족
**위치**: `src/app/game/[code]/page.tsx`

**문제**:
- 복잡한 컴포넌트에서 재렌더링 최적화 부족
- 일부만 useCallback 사용

---

## 🚧 미완성 기능 목록

### 필수 구현
1. ❌ Firebase Realtime Database 위치 동기화
2. ❌ 거리 기반 자동 체포 시스템
3. ❌ 거리 기반 자동 구출 시스템
4. ❌ 경계 이탈 자동 탈락 (15초 규칙)
5. ❌ PWA 아이콘 생성
6. ❌ Service Worker 설정
7. ❌ 에러 처리 강화

### 권장 구현
8. ⚠️ 플레이어 연결 상태 모니터링
9. ⚠️ 게임 도중 방장 위임 로직
10. ⚠️ 재접속 처리
11. ⚠️ 게임 기록 자동 저장 (profileStore 연동)
12. ⚠️ 알림 시스템 개선 (중복 방지)

### 선택 구현
13. 💡 음향 효과 (체포, 구출 시 사운드)
14. 💡 진동 피드백 (Vibration API)
15. 💡 다크/라이트 모드 토글
16. 💡 튜토리얼/온보딩
17. 💡 리더보드

---

## 🔧 성능 이슈

### 1. 지도 재렌더링
**위치**: `src/components/GameMap.tsx`

**문제**:
- 플레이어 위치 업데이트마다 전체 마커 재생성
- 기존 마커 업데이트 대신 삭제 후 재생성

**개선안**:
```typescript
// 기존 마커 위치만 업데이트
const marker = markersRef.current.get(player.id);
if (marker && player.location) {
  marker.setLatLng([player.location.latitude, player.location.longitude]);
} else {
  // 새 마커 생성
}
```

### 2. 위치 업데이트 빈도
**위치**: `src/hooks/useGeolocation.ts`

**문제**:
- `watchPosition`이 변화 감지 시마다 업데이트
- 배터리 소모 증가

**개선안**:
```typescript
// 거리 기반 필터링
const MIN_DISTANCE = 5; // 5m 이상 이동 시만 업데이트
if (calculateDistance(lastLocation, newLocation) > MIN_DISTANCE) {
  updateLocation(newLocation);
}
```

### 3. 게임 루프 최적화 부족
**문제**:
- 모든 플레이어 간 거리 계산 시 O(n²)
- 경찰-도둑 간만 계산하면 충분

---

## 🏗️ 아키텍처 제안

### 1. 실시간 게임 상태 관리
```
Firebase Realtime Database 구조 제안:

games/
  {roomCode}/
    status: "playing"
    startedAt: timestamp
    players/
      {playerId}/
        name: string
        team: "police" | "thief"
        status: "alive" | "caught"
        location:
          latitude: number
          longitude: number
          timestamp: number
        lastSeen: timestamp
```

### 2. 이벤트 기반 아키텍처
```typescript
// 게임 이벤트 중앙 처리
type GameEvent = 
  | { type: 'PLAYER_CAUGHT', policeId, thiefId }
  | { type: 'PLAYER_RESCUED', rescuerId, rescuedId }
  | { type: 'PLAYER_ELIMINATED', playerId, reason }
  | { type: 'GAME_END', winner };

function handleGameEvent(event: GameEvent) {
  // 통합 이벤트 처리
}
```

---

## 📱 PWA 체크리스트

### 현재 상태
- ✅ manifest.json 존재
- ✅ viewport 메타태그 설정
- ✅ theme-color 설정
- ❌ 아이콘 누락
- ❌ Service Worker 없음
- ❌ 오프라인 페이지 없음
- ⚠️ HTTPS 필요 (배포 시)

### 필요한 작업
1. 192x192, 512x512 PNG 아이콘 생성
2. Service Worker 등록
3. 오프라인 폴백 페이지
4. 캐싱 전략 수립

---

## 🔐 보안 고려사항

### 1. Firebase 규칙
**현재**: 규칙 파일 없음

**필요**:
```json
// firestore.rules
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /rooms/{roomId} {
      allow read: if true; // 공개 방 검색
      allow create: if request.auth != null;
      allow update, delete: if request.auth.uid == resource.data.hostId;
    }
  }
}

// database.rules.json (Realtime Database)
{
  "rules": {
    "games": {
      "$roomCode": {
        ".read": true,
        ".write": true,
        "players": {
          "$playerId": {
            ".validate": "newData.hasChildren(['location', 'status'])"
          }
        }
      }
    }
  }
}
```

### 2. 환경변수 노출
**문제**: 클라이언트 사이드 Firebase 키가 노출되는 것은 정상
**권장**: Firebase 콘솔에서 도메인 화이트리스트 설정

---

## 📝 코드 품질 개선 제안

### 1. ESLint 규칙 추가
```json
{
  "extends": ["next/core-web-vitals"],
  "rules": {
    "no-console": "warn",
    "no-magic-numbers": "warn",
    "@typescript-eslint/explicit-function-return-type": "warn"
  }
}
```

### 2. 테스트 추가
```bash
npm install -D @testing-library/react @testing-library/jest-dom jest
```

### 3. Prettier 설정
```json
{
  "semi": true,
  "trailingComma": "es5",
  "singleQuote": false,
  "printWidth": 100
}
```

---

## 🎯 우선순위 수정 작업

### 🔴 최우선 (게임 작동을 위해 필수)
1. Firebase Realtime Database 위치 동기화 구현
2. 거리 기반 자동 체포/구출 로직
3. 경계 이탈 자동 탈락 완성
4. 에러 처리 강화

### 🟡 높음 (배포 전 필수)
5. PWA 아이콘 생성
6. Service Worker 설정
7. Firebase 보안 규칙 작성
8. .env.local 설정 가이드

### 🟢 중간 (사용자 경험 개선)
9. 위치 권한 재요청 UI
10. 재접속 처리
11. 게임 기록 저장
12. 로딩 상태 개선

### 🔵 낮음 (부가 기능)
13. 음향 효과
14. 튜토리얼
15. 테스트 코드 작성

---

## 💡 총평

**장점**:
- 코드 구조가 깔끔하고 TypeScript 사용이 우수함
- UI/UX가 직관적이고 반응형 디자인이 잘 되어 있음
- 프로젝트 설계가 확장 가능하게 되어 있음

**단점**:
- 핵심 멀티플레이어 기능이 미완성
- Firebase 실시간 기능을 거의 사용하지 않음
- PWA로서의 완성도 낮음

**결론**:
프로젝트의 **뼈대는 훌륭**하지만, **실제 게임 로직의 70%가 미구현** 상태입니다.
다음 단계에서 Firebase Realtime Database 통합과 자동 체포/구출 로직을 완성하면
충분히 실용적인 게임이 될 수 있습니다.

**예상 추가 작업 시간**:
- 필수 기능 완성: 8~12시간
- PWA 완성: 2~3시간
- 테스트 및 버그 수정: 4~6시간
- **총 예상 시간: 14~21시간**
